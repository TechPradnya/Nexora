import { Link } from 'react-router-dom'; import { ArrowRight, LockKeyhole, Fingerprint, Scale, ShieldCheck, Bot } from 'lucide-react'; import { useWallet } from '../contexts/WalletContext';
export function Landing(){const {connect,connected}=useWallet();return <div className="landing"><header className="landing-nav"><div className="brand"><div className="brand-mark"><Bot size={20}/></div><b>NEXORA</b></div><div className="landing-actions">{connected?<Link className="button primary" to="/dashboard">Open control plane <ArrowRight size={16}/></Link>:<button className="button ghost" onClick={connect}>Connect Midnight wallet</button>}</div></header><div className="hero"><div className="hero-copy"><div className="kicker"><span className="dot"/> MIDNIGHT-NATIVE TRUST LAYER</div><h1>Trust infrastructure for <span>autonomous agents.</span></h1><p>Nexora lets AI agents establish verifiable identities, enforce policy, coordinate verification, and settle work through privacy-preserving escrow.</p><div className="hero-actions">{connected?<Link className="button primary large" to="/dashboard">Enter Nexora <ArrowRight size={18}/></Link>:<button className="button primary large" onClick={connect}>Connect wallet <ArrowRight size={18}/></button>}<a href="#how" className="button ghost large">See how it works</a></div><div className="hero-note"><ShieldCheck size={16}/> No private keys or seed phrases are requested by Nexora.</div></div><div className="hero-panel"><div className="orb"/><div className="signal-card"><span>SETTLEMENT GATE</span><strong>Policy → Proof → Release</strong><small>State transitions are enforced by Compact circuits.</small></div><div className="mini-grid"><div><Fingerprint/><b>Identity</b><small>Commitment-based</small></div><div><LockKeyhole/><b>Privacy</b><small>Selective disclosure</small></div><div><Scale/><b>Policy</b><small>On-chain rules</small></div></div></div></div><section id="how" className="feature-grid"><Feature icon={<Fingerprint/>} title="Agent identity" text="Bind an agent to a private secret commitment instead of exposing sensitive credentials."/><Feature icon={<Scale/>} title="Policy-governed escrow" text="Minimum reputation, payment limits, roles and verifier requirements become enforceable contract state."/><Feature icon={<ShieldCheck/>} title="Proof-based settlement" text="Deliverables are represented by cryptographic commitments; approval unlocks the settlement path."/></section></div>}
function Feature({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <article className="feature"><div>{icon}</div><h3>{title}</h3><p>{text}</p></article>}

import { useEffect,useState } from 'react'; import { api } from '../lib/api'; import type { Agent } from '../types/domain'; import { EmptyState } from '../components/EmptyState';
export function Reputation(){const [items,setItems]=useState<Agent[]>([]);useEffect(()=>{api.agents().then(x=>setItems(x.items.sort((a,b)=>b.reputation-a.reputation))).catch(()=>setItems([]));},[]);return <><div className="page-intro"><div><p className="eyebrow">TRUST SCORECARD</p><h2>Reputation ledger</h2><p>Reputation should change from settlement outcomes, not from a frontend-only score.</p></div></div>{items.length?<div className="reputation-list">{items.map((a,i)=><div className="reputation-row" key={a.id}><span className="rank">{String(i+1).padStart(2,'0')}</span><div className="avatar">{a.name.slice(0,1)}</div><div className="rep-main"><b>{a.name}</b><small>{a.role} · {a.successfulSettlements} successful / {a.unsuccessfulSettlements} unsuccessful</small></div><div className="rep-score"><strong>{a.reputation}</strong><span>/ 100</span></div></div>)}</div>:<EmptyState title="No reputation records" description="Settlement outcomes will populate the reputation view."/>}</>}

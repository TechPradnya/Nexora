import { useEffect,useState } from 'react'; import { api } from '../lib/api'; import type { Activity as A } from '../types/domain'; import { EmptyState } from '../components/EmptyState'; import { StatusBadge } from '../components/StatusBadge';
export function Activity(){const [items,setItems]=useState<A[]>([]);useEffect(()=>{api.activity().then(x=>setItems(x.items)).catch(()=>setItems([]));},[]);return <><div className="page-intro"><div><p className="eyebrow">AUDIT TRAIL</p><h2>Activity & transactions</h2><p>Blockchain status is displayed only when the application has an actual transaction reference.</p></div></div>{items.length?<div className="activity-list">{items.map(x=><div className="activity-row" key={x.id}><div className="activity-icon">↗</div><div><b>{x.action}</b><small>{new Date(x.timestamp).toLocaleString()} {x.txHash&&<span className="mono">· {x.txHash.slice(0,14)}…</span>}</small></div><StatusBadge status={x.status}/></div>)}</div>:<EmptyState title="No transaction activity" description="Real contract transactions will appear here after indexing."/>}</>}

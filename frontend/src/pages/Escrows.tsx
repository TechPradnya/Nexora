import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { Plus, ArrowRight } from 'lucide-react'; import { api } from '../lib/api'; import type { Escrow } from '../types/domain'; import { StatusBadge } from '../components/StatusBadge'; import { EmptyState } from '../components/EmptyState';
export function Escrows(){const [items,setItems]=useState<Escrow[]>([]);useEffect(()=>{api.escrows().then(x=>setItems(x.items)).catch(()=>setItems([]));},[]);return <><div className="page-intro"><div><p className="eyebrow">ESCROW CONTROL</p><h2>Policy-governed settlements</h2><p>Every payment lifecycle is a state machine: created → funded → verified → released.</p></div><Link className="button primary" to="/escrows/new"><Plus size={16}/> Create escrow</Link></div>{items.length?<div className="table-wrap"><table><thead><tr><th>Escrow</th><th>Parties</th><th>Policy</th><th>Amount</th><th>Status</th><th/></tr></thead><tbody>{items.map(e=><tr key={e.id}><td><b>{e.id.slice(0,16)}…</b><small>{new Date(e.createdAt).toLocaleString()}</small></td><td><small>Client {e.client.slice(0,8)}…</small><small>Contractor {e.contractor.slice(0,8)}…</small></td><td className="mono">{e.policyId.slice(0,10)}…</td><td><b>{e.amount}</b> NXR</td><td><StatusBadge status={e.status}/></td><td><Link to={`/escrows/${e.id}`} className="icon-button"><ArrowRight size={16}/></Link></td></tr>)}</tbody></table></div>:<EmptyState title="No escrows indexed" description="Create and fund the first escrow from the deployed contract." action={<Link className="button ghost" to="/escrows/new">Create escrow</Link>}/>}</>}

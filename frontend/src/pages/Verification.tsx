import { useEffect,useState } from 'react'; import { api } from '../lib/api'; import type { Escrow } from '../types/domain'; import { EmptyState } from '../components/EmptyState'; import { StatusBadge } from '../components/StatusBadge'; import { useWallet } from '../contexts/WalletContext';
export function Verification(){const [items,setItems]=useState<Escrow[]>([]);useEffect(()=>{api.escrows().then(x=>setItems(x.items.filter(e=>['DeliverableSubmitted','UnderVerification'].includes(e.status)))).catch(()=>setItems([]));},[]);const {ctx}=useWallet();async function act(id:string,approve:boolean){if(!ctx){alert('Connect the wallet and configure the contract first.');return;}try{if(approve)await ctx.contract.callTx.approveDeliverable(id);else await ctx.contract.callTx.rejectDeliverable(id);location.reload();}catch(e:any){alert(e.message??'Verification transaction failed.');}}return <><div className="page-intro"><div><p className="eyebrow">INDEPENDENT REVIEW</p><h2>Verifier queue</h2><p>Only the assigned verifier should approve or reject a deliverable.</p></div></div>{items.length?<div className="review-grid">{items.map(e=><article className="review-card" key={e.id}><div className="policy-top"><StatusBadge status={e.status}/><span className="mono">{e.id.slice(0,12)}…</span></div><h3>Deliverable commitment</h3><div className="commitment">{e.commitment??'Not yet indexed'}</div><div className="review-meta"><span>Amount <b>{e.amount} NXR</b></span><span>Policy <b>{e.policyId.slice(0,10)}…</b></span></div><div className="review-actions"><button className="button primary" onClick={()=>act(e.id,true)}>Approve</button><button className="button danger" onClick={()=>act(e.id,false)}>Reject</button></div></article>)}</div>:<EmptyState title="Verification queue is clear" description="Submitted deliverables will appear here when indexed from the contract."/>}</>}
